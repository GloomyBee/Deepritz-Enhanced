# other_method_experiments

Legacy experimental scripts for earlier methods and exploratory runs.

These scripts were moved out of the `examples/` root to keep the main
validation and trial-space workflows easier to scan.

Outputs are aligned under:

- `output/other_method_experiments/`
